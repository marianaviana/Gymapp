<div class="panel-body">
  <%= simple_form_for([@gymcard, @cycle]) do |f| %>
    <div class="form-inputs">
      <%= f.input :name, label: 'Nome:' %>

      <div id='workouts'>
        <%= f.simple_fields_for :workouts do |workout| %>
          <%= render 'workout_fields', :f => workout %>
        <% end %>
        <div class="pull-right">
          <%= link_to_add_association 'Adicionar Exercício', f, :workouts, class: 'btn btn-success' %>
        </div>
      </div>
    </div>
    <br />
    <div class="form-actions">
      <%= f.button :submit, 'Salvar', class: 'btn btn-success' %>
    </div>
  <% end %>
  <br />
  <%= link_to 'Voltar', gymcard_cycles_path(@gymcard) %>
</div>
